import AuthForm from "@/components/AuthForm";
import React from "react";

const page = () => {
  return (
    <section>
      <AuthForm mode="register" />
    </section>
  );
};

export default page;
