import React from "react";
import MarkdownTab from "./MarkdownTab";

const page = () => {
  return (
    <section className="w-full h-full">
      <MarkdownTab />
    </section>
  );
};

export default page;
